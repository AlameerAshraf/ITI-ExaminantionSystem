@using BusineesLayer.Managers
@model ExamMetaData


@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="en" ng-app="Examination">
<head>
    <meta charset="UTF-8">
    <title>Document</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
    <link href="~/Content/assets/css/startexam.css" rel="stylesheet" />

</head>
<body>
    <input type="hidden" name="Id" value=@ViewBag.Std_Id />
    <input type="hidden" name="Track" value=@ViewBag.Track />
    <input type="hidden" name="Branch" value=@ViewBag.Branch />
    <input type="hidden" name="ExId" value=@ViewBag.ExId />
    <input type="hidden" name="CrsId" value=@Model.Course_Id />

    <div class="container">
        <div class="row main-page-form">
            <div class="col-lg-12 title">
                <span>Exam Details</span>
            </div>
            <div class="container-padding">
                <div class="col-md-8">
                    <div class="row">
                        <table class="table">
                            <thead>
                                <tr>
                                    <th class="title-1">Exam name:</th>
                                    <th class="title-2">@Model.Course_Name</th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr>
                                    <td class="title-1">Exam Time:</td>
                                    <td>@Model.Exam_Duration hrs</td>
                                </tr>
                                <tr>
                                    <td class="title-1">Exam Full Mark:</td>
                                    <td>@Model.Exam_Degree</td>
                                </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
                <div class="col-md-4">
                    <img class="img-responsive margin-img" src="~/Content/assets/img/exam.png"alt="">
                </div>
                <div class="col-md-12 text-center margin-btn">
                    <a href="/Student/Exam" class="btn btn-success text-center">Satrt Exam</a>
                </div>
            </div>
        </div>
    </div>
    <!--jquery-->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/1.10.0/jquery.min.js"></script>
    <!--bootstrap-->
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
    <!--angular-->
    <script src="~/Scripts/jQuerySelect.js"></script>
    <script src="~/Scripts/jquery.signalR-2.2.2.min.js"></script>
    <script src="http://localhost:51822/signalr/hubs"></script>
    <script>
        $(document).ready(function () {
            var Std_Id = $("[name='Id']").val();
            var Crs_Id = $("[name='CrsId']").val();
            var Track = $("[name='Track']").val();
            var Branch = $("[name='Branch']").val();
            var ExId = $("[name='ExId']").val();
            // Crs_Id + ExamId + Track + Branch
            var Group = Crs_Id + ExId + Track + Branch;

            //$.connection.hub.url = "http://localhost:51822/signalr";
            //var exam = $.connection.examHub;

            //exam.client.examBroadCasting = function (name, message) {
            //    alert(name + "" + message);
            //}

            //$.connection.hub.start().done(function () {
            //    $("#StartExam").click(function () {
            //        exam.server.studentJoinExamInTiem(Std_Id, Group);
            //    })
            //});

        })
    </script>

</body>
</html>