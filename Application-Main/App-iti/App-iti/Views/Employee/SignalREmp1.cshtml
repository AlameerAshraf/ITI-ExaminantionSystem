
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <script src="~/Scripts/jquery-1.10.2.js"></script>
    <script src="~/Scripts/jquery.signalR-2.2.2.min.js"></script>
    <script src="http://localhost:51822/signalr/hubs"></script>
    <title>SignalREmp1</title>
</head>
<body>
    <div> 
        <input type="button" value="send" id="test" />
    </div>
    <script type="text/javascript">
        $(function () {
            $.connection.hub.url = "http://localhost:51822/signalr";
            var exam = $.connection.examHub;
            var target = 55896;
            $.connection.hub.qs = { "name": "Emplyee1034", "Id": 1034, "Track": "SD" , "Type" : 0 };

            exam.client.broadCast = function (name, message) {
                alert(name + "" + message);
            }

            $.connection.hub.start().done(function () {
                exam.server.send("Alameer", "Hi");
                $("#test").click(function () {
                    exam.server.sendToPerson(target, "message real time w sho8l 3ali, saba7 elfol");
                })
            });
        });
    </script>
</body>

</html>
